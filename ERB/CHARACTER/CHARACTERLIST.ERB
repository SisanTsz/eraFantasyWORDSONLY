@CHARACTERLIST, ARG ;角色列表 1=显示能力
#DIM CHARACTERNUM
PRINTL 角色列表
CUSTOMDRAWLINE -
PRINTFORML [1] %NAME:MASTER%
FOR CHARACTERNUM, 1, 100
    IF EXISTCSV(CHARACTERNUM,0) == 1
        PRINTFORML [{CHARACTERNUM}] %NAME:CHARACTERNUM%
    ENDIF
NEXT
PRINTFORML [99] 返回
INPUT
IF EXISTCSV(RESULT,0) == 1
    CALL CHARACTERLISTCHECK, RESULT ,ARG
ENDIF

@OTHERCHARACTERLIST, ARG ;没有主角 1=切换调教对象，2=更换助手，3=放置调教，4=管理角色
#DIM CHARACTERNUM
PRINTL 角色列表
CUSTOMDRAWLINE -
FOR CHARACTERNUM, 1, 100
    IF EXISTCSV(CHARACTERNUM,0) == 1
        PRINTFORML [{CHARACTERNUM}] %NAME:CHARACTERNUM%
    ENDIF
NEXT
PRINTFORML [99] 返回
INPUT
IF EXISTCSV(RESULT,0) == 1
    CALL OTHERCHARACTERLISTCHECK, RESULT, ARG
ENDIF