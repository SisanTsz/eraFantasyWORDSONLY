@HEADING_1
	CUSTOMDRAWLINE -
	PRINTFORM 第{DAY}天 {FLAG:3}时{FLAG:4}分 所在地:{CFLAG} 
	;CALL DRAWLINES(@"第{DAY}天 {FLAG:3}时{FLAG:4}分 所在地:{CFLAG}")
	IF FLAG:5 == 1
		PRINTFORML %CALLNAME:TARGET%调教中
	ELSE
		PRINTL
	ENDIF
	;CUSTOMDRAWLINE -

@HEADING_MASTER
	CALL DRAWLINES_CENTER("【　状态　】")
	PRINTFORML %NAME:MASTER%
	PRINT 体力:
	CALL PRINT_BAR(BASE:MASTER:0,MAXBASE:MASTER:0,10,　COLOR("体力"))
	PRINTFORM ({BASE:MASTER:0}/{MAXBASE:MASTER:0})
	PRINT 气力:
	CALL PRINT_BAR(BASE:MASTER:1,MAXBASE:MASTER:1,10,　COLOR("气力"))
	PRINTFORML ({BASE:MASTER:1}/{MAXBASE:MASTER:1}) 
	PRINT ＳＰ:
	CALL PRINT_BAR(BASE:MASTER:2,MAXBASE:MASTER:2,10,　COLOR("ＳＰ"))
	PRINTFORM ({BASE:MASTER:2}/{MAXBASE:MASTER:2})
	PRINT 精力:
	CALL PRINT_BAR(BASE:MASTER:3,MAXBASE:MASTER:3,10,　COLOR("精力"))
	PRINTFORML ({BASE:MASTER:3}/{MAXBASE:MASTER:3})
@HEADING_TARGET, ARG
	IF ARG >= 0
    	PRINTFORML %NAME:ARG% 好感:{CFLAG:ARG:6}
    	PRINT 体力:
		CALL PRINT_BAR(BASE:ARG:0,MAXBASE:ARG:0,10,　COLOR("体力"))
		PRINTFORM ({BASE:ARG:0}/{MAXBASE:ARG:0})
		PRINT 气力:
		CALL PRINT_BAR(BASE:ARG:1,MAXBASE:ARG:1,10,　COLOR("气力"))
		PRINTFORML ({BASE:ARG:1}/{MAXBASE:ARG:1}) 
		PRINT ＳＰ:
		CALL PRINT_BAR(BASE:ARG:2,MAXBASE:ARG:2,10,　COLOR("ＳＰ"))
		PRINTFORM ({BASE:ARG:2}/{MAXBASE:ARG:2})
		PRINT 理智:
		CALL PRINT_BAR(BASE:ARG:4,MAXBASE:ARG:4,10,　COLOR("理智"))
		PRINTFORML ({BASE:ARG:4}/{MAXBASE:ARG:4})
	ENDIF

@PRINT_BAR(ARG:1, ARG:2, ARG:3, ARG:4);当前值，最大值，长度，颜色
	#DIM LCOUNT
	#DIM BAR_COLOR
	#DIM BG_COLOR
	#DIMS BAR_CHARA
	#DIMS INS_CHARA

	SIF ARG:1 < 0
		ARG:1 = 0
	SIF ARG:2 < 0
		ARG:2 = 0

	PRINTPLAINFORM [

	BAR_CHARA = %UNICODE(0x2587)%

	BAR_COLOR = ARG:4

	SETCOLOR BAR_COLOR

	INS_CHARA = ARG:3

	BG_COLOR = (MAX(30, (BAR_COLOR/65536)/5)*65536 + MAX(30, ((BAR_COLOR/256)%256)/5)*256 + MAX(30, (BAR_COLOR%256)/5) )

	FOR LCOUNT, 0, ARG:3
		SIF ((ARG:1 * ARG:3 + ARG:2 - 1) / ARG:2) <= LCOUNT
			SETCOLOR BG_COLOR
			PRINTPLAINFORM %BAR_CHARA%
	NEXT

	RESETCOLOR

	PRINTPLAINFORM ]


@DRAWLINES(ARGS)
	#DIM LCOUNT
	#DIMS BUTTONS

	BUTTONS = %ARGS%

	PRINTBUTTON ARGS, BUTTONS

	FOR LCOUNT, 0, NUM("一行字数") - STRLENS(ARGS)
		PRINTFORM -
	NEXT
	;SIF (NUM("一行字数") - STRLENS(ARGS)) % 2 != 1
		;PRINTFORM -
	PRINTL 

@DRAWLINES_CENTER(ARGS)
	#DIM LCOUNT
	FOR LCOUNT, 0, (NUM("一行字数") - STRLENS(ARGS) )/2
		PRINTFORM -
	NEXT
	PRINTFORM %ARGS%
	FOR LCOUNT, 0, (NUM("一行字数") - STRLENS(ARGS) )/2
		PRINTFORM -
	NEXT
	PRINTL
