@TRAIN_CHECK, ARG ;调教指令转移
    #DIM COMF_NUM
    IF ARG >= 1 && ARG <= 800
        CALLFORM COM{ARG}
    ELSEIF RESULT == 901
        CALLFORM ABILITY_SHOW(TARGET)
    ELSEIF RESULT == 902
        CALL STATUS_SHOW
    ELSEIF RESULT == 903
        BEGIN AFTERTRAIN
        BEGIN ABLUP
    ENDIF