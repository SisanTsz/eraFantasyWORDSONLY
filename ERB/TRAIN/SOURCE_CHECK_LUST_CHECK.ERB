@SOURCE_CHECK_LUST_CHECK
    #DIM LCOUNT
    FOR LCOUNT, 0, 8
        IF SOURCE:LCOUNT >= 10000
            BASE:TARGET:5 += 2
        ELSEIF SOURCE:LCOUNT >= 5000
            BASE:TARGET:5 += 1
        ENDIF
    NEXT

    SIF SOURCE:情爱 >= 5000
        BASE:TARGET:5 += 1
        
    SIF SOURCE:欲情 >= 10000
        BASE:TARGET:5 += 1

    SIF BASE:TARGET:5 >= MAXBASE:TARGET:5
        BASE:TARGET:5 = MAXBASE:TARGET:5