@USERCOM
    SELECTCASE RESULT
        CASE 2003
            IF TFLAG:303 == 1
                TFLAG:303 = 0
            ELSE
                TFLAG:303 = 1
            ENDIF
    ENDSELECT
    
@EVENTEND ;调教结束
    BEGIN ABLUP

    ;CALL ABL_AFTERTRAIN_MAIN