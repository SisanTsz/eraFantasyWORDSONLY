@USERCOM
    SELECTCASE RESULT
        CASE 2003
            IF TFLAG:单发连发 == 1
                TFLAG:单发连发 = 0
            ELSE
                TFLAG:单发连发 = 1
            ENDIF
        CASE 2051
            IF TFLAG:忍耐中 == 1
                TFLAG:忍耐中 = 0
            ELSE
                TFLAG:忍耐中 = 1
            ENDIF
        CASE 2052
            IF TFLAG:寸止中 == 1
                TFLAG:寸止中 = 0
            ELSE
                TFLAG:寸止中 = 1
            ENDIF
    ENDSELECT
    
@EVENTEND ;调教结束
    BEGIN ABLUP

    ;CALL ABL_AFTERTRAIN_MAIN