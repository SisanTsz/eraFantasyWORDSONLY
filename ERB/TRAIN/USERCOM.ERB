@USERCOM
    IF RESULT == 901
        CALL ABILITY_SHOW(TARGET,1)
    ELSEIF RESULT == 902
        IF TFLAG:0 == 1
            TFLAG:0 = 0
        ELSE
            TFLAG:0 = 1
        ENDIF
    ELSEIF RESULT == 903
        BEGIN ABLUP
        RETURN 1
    ENDIF
    RETURN 0

@EVENTEND ;调教结束
    BEGIN ABLUP

    ;CALL ABL_AFTERTRAIN_MAIN