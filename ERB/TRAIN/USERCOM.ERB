@USERCOM
    IF RESULT == 901
        CALL ABILITY_SHOW(TARGET)
    ELSEIF RESULT == 902
        IF TFLAG:0 == 1
            TFLAG:0 = 0
        ELSE
            TFLAG:0 = 1
        ENDIF
    ELSEIF RESULT == 903
        BEGIN ABLUP
        RETURN 1
    ENDIF
    RETURN 0

@EVENTEND