@SOURCE_CHECK_C
    IF TALENT:C性感 == 1
        TIMES SOURCE:快C , 0.5
    ELSEIF TALENT:C性感 == 2
        TIMES SOURCE:快C , 1.5
    ELSEIF TALENT:C性感 == 3
        TIMES SOURCE:快C , 2
    ENDIF

    IF TALENT:接受快感 == 1
        TIMES SOURCE:快C , 1.5
    ELSEIF TALENT:接受快感 == 2
        TIMES SOURCE:快C , 0.5
    ENDIF

    UP:2 += SOURCE:快C