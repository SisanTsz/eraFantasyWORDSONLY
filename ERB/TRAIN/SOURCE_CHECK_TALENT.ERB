@SOURCE_CHECK_TALENT
    #DIM LCOUNT
    ;淫乱
    IF TALENT:TARGET:淫乱
        FOR LCOUNT, 1, 10
            TIMES SOURCE:LCOUNT, 1.2
        NEXT
    ENDIF

    ;否认快感
    IF TALENT:TARGET:否认快感
        FOR LCOUNT, 1, 10
            TIMES SOURCE:LCOUNT, 0.85
        NEXT
    ENDIF
