@TRAIN_WEARING(ARG)
    #DIM LCOUNT 
    #DIM WTARGET
    WTARGET = ARG
    FOR LCOUNT, 1, 11
        TRYCALLFORM TRAIN_WEARING_{LCOUNT}(WTARGET)
    NEXT

@TRAIN_WEARING_1(ARG)
    #DIM WTARGET
    #DIM LCOUNT
    SELECTCASE CFLAG:WTARGET:
        CASE 1
            CALL 
    ENDSELECT

