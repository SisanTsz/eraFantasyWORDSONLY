@ABLUP_COND304
    #DIM JUEL_NEEDED
    #DIM EXP_NEEDED
    PRINTFORML 献身 LV{ABL:TARGET:献身+1}
    PRINTL 献身描述
    PRINTL
    JUEL_NEEDED = ABLUP_JUEL_NEEDED("献身")
    EXP_NEEDED = ABLUP_EXP_NEEDED("献身")
    SELECTCASE ABL:TARGET:献身
        CASE 0 TO 4
            PRINTFORML 情爱之珠 {JUEL:TARGET:情爱}/{JUEL_NEEDED}
            PRINTFORML 奉侍快乐经验 {EXP:TARGET:奉侍快乐经验}/{EXP_NEEDED}
            PRINTL
        CASE 5 TO 9
            PRINTFORML 情爱之珠 {JUEL:TARGET:情爱}/{JUEL_NEEDED}
            PRINTFORML 奉侍快乐经验 {EXP:TARGET:奉侍快乐经验}/{EXP_NEEDED}
            PRINTFORML 盲信特质 {TALENT:TARGET:盲信}/1
    ENDSELECT
    PRINTL
    SELECTCASE ABL:TARGET:献身
        CASE 0 TO 4
            IF JUEL:TARGET:情爱 > JUEL_NEEDED && EXP:TARGET:绝顶经验 > EXP_NEEDED
                PRINTFORML [ 998] 确认升级
            ELSE
                PRINTL [    ] 条件不足
            ENDIF
        CASE 5 TO 9
            IF JUEL:TARGET:情爱 > JUEL_NEEDED && EXP:TARGET:绝顶经验 > EXP_NEEDED && TALENT:TARGET:盲信 != 0
                PRINTFORML [ 998] 确认升级
            ELSE
                PRINTL [    ] 条件不足
            ENDIF
    ENDSELECT

@ABLUP304_CONFIRM
    #DIM JUEL_NEEDED
    #DIM EXP_NEEDED
    JUEL_NEEDED = ABLUP_JUEL_NEEDED("献身")
    EXP_NEEDED = ABLUP_EXP_NEEDED("献身")
    CUSTOMDRAWLINE -
    SELECTCASE ABL:TARGET:献身
        CASE 0 TO 4
            IF JUEL:TARGET:情爱 > JUEL_NEEDED && EXP:TARGET:绝顶经验 > EXP_NEEDED
                JUEL:TARGET:情爱 -= JUEL_NEEDED
                ABL:TARGET:献身 += 1
            ENDIF
        CASE 5 TO 9
            IF JUEL:TARGET:情爱 > JUEL_NEEDED && EXP:TARGET:绝顶经验 > EXP_NEEDED && TALENT:TARGET:盲信 != 0
                JUEL:TARGET:情爱 -= JUEL_NEEDED
                ABL:TARGET:献身 += 1
            ENDIF
    ENDSELECT
@IS_ABLUP304
    #DIM JUEL_NEEDED
    #DIM EXP_NEEDED
    #DIMS COND
    JUEL_NEEDED = ABLUP_JUEL_NEEDED("献身")
    EXP_NEEDED = ABLUP_EXP_NEEDED("献身")
    SELECTCASE ABL:TARGET:献身
        CASE 0 TO 4
            IF JUEL:TARGET:情爱 >= JUEL_NEEDED && EXP:TARGET:奉侍快乐经验 > EXP_NEEDED
                COND = ◯
            ELSE
                COND = 
            ENDIF
        CASE 5 TO 9
            IF TALENT:TARGET:盲信 == 0
                COND = 
            ELSE
                IF JUEL:TARGET:情爱 >= JUEL_NEEDED && EXP:TARGET:奉侍快乐经验 > EXP_NEEDED
                    COND = ◯
                ELSE
                    COND =
                ENDIF
            ENDIF
    ENDSELECT
    PRINTFORM %COND%